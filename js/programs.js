document.addEventListener("DOMContentLoaded", () => {
  // Any remaining program-related functionality can go here
  // Currently, there's no need for any specific functionality
  // after removing the modals
})

